<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Beri Nilai - LABCORNER</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Poppins', sans-serif; }
    </style>
</head>
<body class="bg-[#F9F5FF]">
    <div class="flex h-screen">
        <!-- Sidebar -->
        <%- include('partials/sidebar', { praktikum }) %>

        <!-- Main Content -->
        <div class="flex-1 flex flex-col overflow-y-auto">
            <!-- Header -->
            <header class="flex items-center justify-between py-3 px-6 bg-white shadow-sm sticky top-0 z-20">
                <h2 class="text-xl font-semibold text-gray-800">LABCORNER</h2>
                <div class="flex items-center space-x-5">
                    <img src="https://i.ibb.co/68q746Z/logo-web.png" alt="Logo" class="h-8 w-8 rounded-full">
                    <button class="text-gray-500 relative">
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 17h5l-1.405-1.405A2.032 2.032 0 0118 14.158V11a6 6 0 10-12 0v3.159c0 .538-.214 1.055-.595 1.436L4 17h5m6 0v1a3 3 0 11-6 0v-1m6 0H9" /></svg>
                    </button>
                    <div class="relative">
                        <img class="h-9 w-9 rounded-full" src="https://i.ibb.co/6n20s3v/4215763.png" alt="User profile">
                    </div>
                </div>
            </header>

            <!-- Content Area -->
            <main class="flex-1 p-10">
                <% if (pengumpulan) { %>
                    <div class="bg-white rounded-2xl shadow-lg p-8">
                        <h1 class="text-2xl font-bold text-gray-800 mb-6"><%= pengumpulan.tugas.judul %></h1>

                        <div class="bg-[#F3E8FF] rounded-xl p-6">
                            <!-- Info Mahasiswa dan Waktu -->
                            <div class="flex justify-between items-center mb-6">
                                <div class="flex items-center gap-4">
                                    <div class="bg-gray-200 p-2 rounded-full">
                                        <svg class="w-6 h-6 text-gray-600" fill="currentColor" viewBox="0 0 20 20"><path fill-rule="evenodd" d="M10 9a3 3 0 100-6 3 3 0 000 6zm-7 9a7 7 0 1114 0H3z" clip-rule="evenodd"></path></svg>
                                    </div>
                                    <div>
                                        <p class="font-bold text-gray-800"><%= pengumpulan.user.username %></p>
                                        <p class="text-sm text-gray-500"><%= pengumpulan.user.id %></p> <!-- atau info lain dari kolom user -->
                                    </div>
                                </div>
                                <div class="text-right">
                                    <% const submissionDate = new Date(pengumpulan.dikumpulkan_pada); %>
                                    <p class="text-sm font-medium text-gray-600"><%= submissionDate.toLocaleDateString('id-ID', { weekday: 'long', day: 'numeric', month: 'long', year: 'numeric' }) %></p>
                                    <p class="text-sm text-gray-500"><%= submissionDate.toLocaleTimeString('id-ID', { hour: '2-digit', minute: '2-digit' }).replace('.',':') %></p>
                                </div>
                            </div>

                            <!-- File yang Dikumpulkan -->
                            <% if (pengumpulan.path_file) { %>
                                <div class="mb-8">
                                    <h3 class="text-lg font-bold text-gray-700 mb-2">File yang Dikumpulkan</h3>
                                    <a href="/download/<%= encodeURIComponent(pengumpulan.path_file) %>" class="inline-flex items-center gap-3 bg-purple-100 border border-purple-300 rounded-lg px-4 py-3 hover:bg-purple-200 transition-colors">
                                        <svg class="h-6 w-6 text-purple-700" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
                                        </svg>
                                        <span class="text-purple-800 font-medium"><%= pengumpulan.nama_file %></span>
                                    </a>
                                </div>
                            <% } %>

                            <!-- Form Penilaian -->
                            <form action="/assignments/nilai/<%= pengumpulan.id %>" method="POST">
                                <div class="grid grid-cols-1 md:grid-cols-4 gap-6">
                                    <div class="md:col-span-1">
                                        <label for="nilai" class="block text-sm font-bold text-gray-700 mb-2">Nilai</label>
                                        <input type="number" id="nilai" name="nilai" value="<%= pengumpulan.nilai ?? '' %>" class="w-full px-4 py-3 bg-white border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-purple-500" min="0" max="100">
                                    </div>
                                    <div class="md:col-span-3">
                                        <label for="catatan" class="block text-sm font-bold text-gray-700 mb-2">Komentar</label>
                                        <textarea id="catatan" name="catatan" rows="4" class="w-full px-4 py-3 bg-white border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-purple-500" placeholder="Beri masukan atau komentar..."><%= pengumpulan.catatan ?? '' %></textarea>
                                    </div>
                                </div>
                                <div class="mt-8 flex justify-end gap-4">
                                    <a href="/assignments/<%= pengumpulan.tugas.id %>/pengumpulan" class="px-8 py-3 bg-gray-200 text-gray-800 font-bold rounded-lg hover:bg-gray-300">Batal</a>
                                    <button type="submit" class="px-8 py-3 bg-gray-800 text-white font-bold rounded-lg hover:bg-black border border-gray-300">Simpan</button>
                                </div>
                            </form>
                        </div>
                    </div>
                <% } else { %>
                    <p class="text-center text-gray-600">Data pengumpulan tidak ditemukan.</p>
                <% } %>
            </main>
        </div>
    </div>
</body>
</html>