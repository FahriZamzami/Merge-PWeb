<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.2.0/css/all.min.css">
    <title><%= title %></title>
</head>
<body class="bg-purple-50">
    <div class="flex h-screen">
        <%- include('partials/sidebar', { praktikum: praktikum, currentPath: currentPath }) %>

        <main class="flex-1 p-4 sm:p-8 overflow-y-auto">
            <header class="flex justify-between items-center mb-8">
                <h1 class="text-3xl font-bold text-gray-800">Nilai Mahasiswa</h1>
                <div x-data="{ open: false }" class="relative">
                    <button @click="open = !open" class="bg-purple-600 text-white font-bold py-2 px-6 rounded-lg hover:bg-purple-700 transition duration-300 flex items-center">
                        <i class="fas fa-download mr-2"></i>Unduh <i class="fas fa-chevron-down ml-2 text-xs"></i>
                    </button>
                    <div x-show="open" @click.away="open = false" x-transition class="absolute right-0 mt-2 w-48 bg-white rounded-md shadow-lg py-1 z-10">
                        <a href="/kuis/<%= kuis.id %>/nilai/excel" class="block px-4 py-2 text-sm text-gray-700 hover:bg-gray-100">
                            <i class="fas fa-file-excel mr-2 text-green-500"></i>Unduh Excel (.xlsx)
                        </a>
                        <a href="/kuis/<%= kuis.id %>/nilai/pdf" class="block px-4 py-2 text-sm text-gray-700 hover:bg-gray-100">
                            <i class="fas fa-file-pdf mr-2 text-red-500"></i>Unduh PDF (.pdf)
                        </a>
                    </div>
                </div>
            </header>
            
            <div class="bg-white rounded-xl shadow-lg p-6">
                <div class="grid grid-cols-4 gap-4 text-left text-sm font-semibold text-gray-500 uppercase px-4 mb-4">
                    <div>Nama</div>
                    <div>Nim</div>
                    <div>Waktu Submit</div>
                    <div>Nilai</div>
                </div>

                <div class="space-y-3">
                    <% if (daftarNilai.length > 0) { %>
                        <% daftarNilai.forEach(nilai => { %>
                            <div class="grid grid-cols-4 gap-4 items-center bg-gray-50 hover:bg-gray-100 p-4 rounded-lg transition">
                                <div class="font-medium text-gray-800"><%= nilai.user.id %></div>
                                <div class="text-gray-600"><%= nilai.user.username %></div>
                                <div class="text-gray-600"><%= new Date(nilai.waktu_selesai).toLocaleTimeString('id-ID', { hour: '2-digit', minute: '2-digit' }) %></div>
                                <div class="font-semibold text-gray-800"><%= nilai.total_poin %> / <%= maxPoin %></div>
                            </div>
                        <% }) %>
                    <% } else { %>
                        <div class="text-center py-10 text-gray-500">
                            <p>Belum ada mahasiswa yang menyelesaikan kuis ini.</p>
                        </div>
                    <% } %>
                </div>
            </div>

             <div class="mt-8 flex justify-end">
                <a href="/praktikum/<%= praktikum.id %>/kuis" class="bg-gray-200 text-gray-800 font-bold py-2 px-6 rounded-lg hover:bg-gray-300 transition">
                    Kembali
                </a>
            </div>
        </main>
    </div>
    <script defer src="https://unpkg.com/alpinejs@3.x.x/dist/cdn.min.js"></script>
</body>
</html> 