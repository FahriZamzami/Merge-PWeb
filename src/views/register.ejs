<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Sign Up - LABCORNER</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Poppins', sans-serif; }
        .custom-placeholder::placeholder { color: #A1A1AA; }
    </style>
</head>
<body>
    <div class="flex min-h-screen">
        <div class="w-full lg:w-1/2 bg-gradient-to-br from-[#6D28D9] to-[#D8B4FE] flex flex-col justify-center items-center p-8 sm:p-12">
            <div class="w-full max-w-md">
                <div class="flex items-center space-x-8 mb-10">
                    <a href="/login" class="text-xl font-medium text-white pb-2">Sign In</a>
                    <a href="/register" class="text-xl font-bold text-white pb-2 border-b-4 border-white">Sign Up</a>
                </div>

                <% if (step === 1) { %>
                    <h2 class="text-3xl font-bold text-white mb-8">Create An Account</h2>
                    <% if (typeof error !== 'undefined' && error) { %>
                        <div class="mb-4 p-3 bg-red-100 text-red-700 rounded-lg text-sm">
                            <%= error %>
                        </div>
                    <% } %>
                    <form action="/register" method="POST">
                        <div class="mb-6">
                            <input type="text" name="nama" placeholder="Nama" class="w-full px-5 py-4 bg-white/80 rounded-xl shadow-inner focus:outline-none focus:ring-2 focus:ring-purple-500 text-gray-900 custom-placeholder" required>
                        </div>
                        <div class="mb-8">
                            <input type="text" name="nim" placeholder="NIM" class="w-full px-5 py-4 bg-white/80 rounded-xl shadow-inner focus:outline-none focus:ring-2 focus:ring-purple-500 text-gray-900 custom-placeholder" required>
                        </div>
                        <button type="submit" class="w-full px-10 py-3 bg-gray-900 text-white font-semibold rounded-xl shadow-md hover:bg-gray-800 focus:outline-none">
                            Buat Password
                        </button>
                    </form>

                <% } else if (step === 2) { %>
                    <h2 class="text-3xl font-bold text-white mb-8">Buat Password</h2>

                    <% if (typeof error !== 'undefined' && error) { %>
                        <div class="mb-4 p-3 bg-red-100 text-red-700 rounded-lg text-sm">
                            <%= error %>
                        </div>
                    <% } %>

                    <form action="/register/create" method="POST">
                        <input type="hidden" name="nama" value="<%= typeof nama !== 'undefined' ? nama : '' %>">
                        <input type="hidden" name="nim" value="<%= typeof nim !== 'undefined' ? nim : '' %>">

                        <!-- Password -->
                        <div class="mb-6 relative">
                            <input type="password" name="password" id="password" placeholder="Password"
                                class="w-full px-5 py-4 bg-white/80 rounded-xl shadow-inner focus:outline-none focus:ring-2 focus:ring-purple-500 text-gray-900 custom-placeholder"
                                required>
                            <button type="button" onclick="togglePassword('password', this)"
                                class="absolute inset-y-0 right-4 flex items-center text-gray-500 hover:text-gray-700">
                                <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none"
                                    viewBox="0 0 24 24" stroke="currentColor">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                        d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" />
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                        d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z" />
                                </svg>
                            </button>
                        </div>

                        <!-- Verifikasi Password -->
                        <div class="mb-8 relative">
                            <input type="password" name="verifikasi_password" id="verifikasi_password" placeholder="Verifikasi Password"
                                class="w-full px-5 py-4 bg-white/80 rounded-xl shadow-inner focus:outline-none focus:ring-2 focus:ring-purple-500 text-gray-900 custom-placeholder"
                                required>
                            <button type="button" onclick="togglePassword('verifikasi_password', this)"
                                class="absolute inset-y-0 right-4 flex items-center text-gray-500 hover:text-gray-700">
                                <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none"
                                    viewBox="0 0 24 24" stroke="currentColor">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                        d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" />
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                        d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z" />
                                </svg>
                            </button>
                        </div>

                        <p class="text-xs text-white mb-8">Gunakan minimal 8 karakter dengan campuran huruf dan angka</p>
                        <button type="submit" class="w-full px-10 py-3 bg-gray-900 text-white font-semibold rounded-xl shadow-md hover:bg-gray-800 focus:outline-none">
                            Buat Akun
                        </button>
                    </form>
                <% } %>
            </div>
        </div>

        <!-- Ilustrasi -->
        <div class="hidden lg:flex w-3/5 bg-white flex-col justify-center items-center p-12 text-center">
            <img src="/assets/image/login-image.png" alt="Illustration" class="w-full max-w-lg mx-auto">
            <p class="text-lg text-gray-600 mt-8">"Satu pintu untuk semua kebutuhan praktikum Anda"</p>
        </div>
    </div>

    <!-- Script untuk toggle mata password -->
    <script>
        function togglePassword(fieldId, btn) {
            const input = document.getElementById(fieldId);
            const svg = btn.querySelector('svg');

            if (input.type === 'password') {
                input.type = 'text';
                svg.innerHTML = `
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                        d="M13.875 18.825A10.05 10.05 0 0112 19c-4.478 0-8.268-2.943-9.542-7a9.969 9.969 0 011.507-2.748M9.88 9.88a3 3 0 014.24 4.24M6.1 6.1l11.8 11.8" />
                `;
            } else {
                input.type = 'password';
                svg.innerHTML = `
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                        d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" />
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                        d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z" />
                `;
            }
        }
    </script>
</body>
</html>