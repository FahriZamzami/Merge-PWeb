<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body class="bg-gradient-to-br from-red-50 to-orange-50 min-h-screen">

<!-- Header -->
<header class="bg-white/90 backdrop-blur-lg shadow-lg border-b border-red-200">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between items-center py-6">
      <div class="flex items-center space-x-4">
        <a href="/" class="bg-gradient-to-r from-purple-600 to-indigo-600 hover:from-purple-700 hover:to-indigo-700 text-white font-bold px-4 py-2 rounded-lg shadow-lg transition-all transform hover:scale-105 active:scale-95 flex items-center space-x-2">
          <i class="fas fa-arrow-left"></i>
          <span>Back to Dashboard</span>
        </a>
        <div>
          <h1 class="text-2xl font-bold bg-gradient-to-r from-red-600 to-orange-600 bg-clip-text text-transparent">
            Website Shutdown
          </h1>
          <p class="text-sm text-gray-600">Control system status</p>
        </div>
      </div>
      
      <div class="flex items-center space-x-2">
        <div class="w-3 h-3 bg-red-500 rounded-full animate-pulse"></div>
        <span class="text-sm font-medium text-red-700">System Control</span>
      </div>
    </div>
  </div>
</header>

<main class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
  
  <% if (isOffline) { %>
    <!-- Website is OFF -->
    <div class="bg-white rounded-2xl shadow-lg border border-red-200 p-8 text-center">
      <div class="w-24 h-24 bg-red-100 rounded-full flex items-center justify-center mx-auto mb-6">
        <i class="fas fa-power-off text-red-500 text-4xl"></i>
      </div>
      
      <h2 class="text-3xl font-bold text-red-600 mb-4">Website Sudah Dimatikan</h2>
      <p class="text-gray-600 text-lg mb-8">
        Sistem telah di-shutdown dan tidak dapat diakses oleh pengguna. 
        Hanya admin yang dapat menghidupkan kembali.
      </p>
      
      <div class="bg-red-50 border border-red-200 rounded-xl p-6 mb-8">
        <h3 class="text-lg font-semibold text-red-800 mb-3">Status Sistem:</h3>
        <div class="space-y-2 text-sm text-red-700">
          <div class="flex items-center justify-between">
            <span>Website Access:</span>
            <span class="font-semibold">❌ Disabled</span>
          </div>
          <div class="flex items-center justify-between">
            <span>Database:</span>
            <span class="font-semibold">❌ Disconnected</span>
          </div>
          <div class="flex items-center justify-between">
            <span>API Services:</span>
            <span class="font-semibold">❌ Stopped</span>
          </div>
        </div>
      </div>
      
      <form action="/matikan/aktifkan" method="POST" class="space-y-4">
        <div class="bg-yellow-50 border border-yellow-200 rounded-xl p-4">
          <p class="text-yellow-800 text-sm">
            <i class="fas fa-exclamation-triangle mr-2"></i>
            <strong>Admin Only:</strong> Masukkan kode admin untuk menghidupkan website kembali
          </p>
        </div>
        
        <div class="max-w-md mx-auto">
          <input type="password" name="adminCode" placeholder="Masukkan kode admin" 
                 class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-red-500 focus:border-transparent transition-all"
                 required>
        </div>
        
        <button type="submit" class="bg-gradient-to-r from-green-500 to-green-600 hover:from-green-600 hover:to-green-700 text-white font-semibold px-8 py-3 rounded-lg shadow-lg transition-all transform hover:scale-105 active:scale-95">
          <i class="fas fa-play mr-2"></i>
          Hidupkan Website
        </button>
      </form>
    </div>
    
  <% } else { %>
    <!-- Website is ON -->
    <div class="bg-white rounded-2xl shadow-lg border border-green-200 p-8 text-center">
      <div class="w-24 h-24 bg-green-100 rounded-full flex items-center justify-center mx-auto mb-6">
        <i class="fas fa-check-circle text-green-500 text-4xl"></i>
      </div>
      
      <h2 class="text-3xl font-bold text-green-600 mb-4">Website Aktif</h2>
      <p class="text-gray-600 text-lg mb-8">
        Sistem sedang berjalan normal dan dapat diakses oleh semua pengguna.
      </p>
      
      <div class="bg-green-50 border border-green-200 rounded-xl p-6 mb-8">
        <h3 class="text-lg font-semibold text-green-800 mb-3">Status Sistem:</h3>
        <div class="space-y-2 text-sm text-green-700">
          <div class="flex items-center justify-between">
            <span>Website Access:</span>
            <span class="font-semibold">✅ Enabled</span>
          </div>
          <div class="flex items-center justify-between">
            <span>Database:</span>
            <span class="font-semibold">✅ Connected</span>
          </div>
          <div class="flex items-center justify-between">
            <span>API Services:</span>
            <span class="font-semibold">✅ Running</span>
          </div>
        </div>
      </div>
      
      <div class="bg-red-50 border border-red-200 rounded-xl p-6 mb-8">
        <h3 class="text-lg font-semibold text-red-800 mb-3">⚠️ Peringatan</h3>
        <p class="text-red-700 text-sm mb-4">
          Mematikan website akan menghentikan semua layanan dan tidak dapat diakses oleh pengguna.
          Pastikan semua data penting telah disimpan.
        </p>
        
        <form action="/matikan" method="POST" class="space-y-4">
          <div class="flex items-center justify-center space-x-4">
            <input type="checkbox" id="confirmShutdown" name="confirmShutdown" required 
                   class="w-4 h-4 text-red-600 border-gray-300 rounded focus:ring-red-500">
            <label for="confirmShutdown" class="text-sm text-red-700">
              Saya mengerti dan ingin mematikan website
            </label>
          </div>
          
          <button type="submit" class="bg-gradient-to-r from-red-500 to-red-600 hover:from-red-600 hover:to-red-700 text-white font-semibold px-8 py-3 rounded-lg shadow-lg transition-all transform hover:scale-105 active:scale-95">
            <i class="fas fa-power-off mr-2"></i>
            Matikan Website
          </button>
        </form>
      </div>
    </div>
  <% } %>
  
  <!-- System Information -->
  <div class="bg-white rounded-2xl shadow-lg border border-gray-200 p-6 mt-8">
    <h3 class="text-xl font-bold text-gray-800 mb-4">Informasi Sistem</h3>
    <div class="grid grid-cols-1 md:grid-cols-3 gap-4 text-sm">
      <div class="p-4 bg-gray-50 rounded-lg">
        <p class="font-semibold text-gray-700">Server Time</p>
        <p class="text-gray-600" id="serverTime">Loading...</p>
      </div>
      <div class="p-4 bg-gray-50 rounded-lg">
        <p class="font-semibold text-gray-700">System Status</p>
        <p class="text-gray-600"><%= isOffline ? 'Offline' : 'Online' %></p>
      </div>
      <div class="p-4 bg-gray-50 rounded-lg">
        <p class="font-semibold text-gray-700">Last Updated</p>
        <p class="text-gray-600" id="lastUpdated">Loading...</p>
      </div>
    </div>
  </div>
</main>

<script>
// Update server time
function updateTime() {
  const now = new Date();
  document.getElementById('serverTime').textContent = now.toLocaleString('id-ID');
  document.getElementById('lastUpdated').textContent = now.toLocaleString('id-ID');
}

updateTime();
setInterval(updateTime, 1000);

// Confirmation for shutdown
document.querySelector('form[action="/matikan"]')?.addEventListener('submit', function(e) {
  if (!confirm('Anda yakin ingin mematikan website? Semua layanan akan berhenti dan pengguna tidak dapat mengakses sistem.')) {
    e.preventDefault();
  }
});

// Admin code validation (you can implement proper validation here)
document.querySelector('form[action="/matikan/aktifkan"]')?.addEventListener('submit', function(e) {
  const adminCode = document.querySelector('input[name="adminCode"]').value;
  if (!adminCode || adminCode.length < 3) {
    e.preventDefault();
    alert('Kode admin tidak valid!');
  }
});
</script>
</body>
</html>
