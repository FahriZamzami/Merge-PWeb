<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detail Penugasan - LABCORNER</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Poppins', sans-serif;
        }
    </style>
</head>
<body class="bg-[#F9F5FF]">
    <div class="flex h-screen">
        <%- include('partials/sidebar', { praktikum }) %>

        <div class="flex-1 flex flex-col overflow-y-auto">
            <main class="flex-1 p-5">
                <% if (assignment) { %>
                    <% const isClosed = assignment.status === 'close'; %>
                    <div class="bg-white rounded-2xl shadow-lg p-8">
                        <div class="mt-6 text-left pb-5">
                            <a href="/assignments" class="text-purple-600 hover:underline font-medium">
                                ← Kembali
                            </a>
                        </div>
                        <div class="flex justify-between items-start mb-6">
                            <div>
                                <h2 class="text-3xl font-bold text-gray-800"><%= assignment.judul %></h2>
                                <p class="text-sm text-gray-500 mt-1"><%= praktikum.nama_praktikum %> <%= praktikum.kelas %></p>
                            </div>
                            <span class="px-4 py-1.5 text-sm font-semibold rounded-full <%= isClosed ? 'bg-red-100 text-red-800' : 'bg-green-100 text-green-800' %>">
                                <%= isClosed ? 'Penugasan Ditutup' : 'Penugasan Dibuka' %>
                            </span>
                        </div>

                        <div class="mb-6">
                            <h3 class="text-lg font-bold text-gray-700 mb-2">Deskripsi</h3>
                            <p class="text-gray-600 prose"><%= assignment.deskripsi %></p>
                        </div>
                        
                        <% if (assignment.nama_file) { %>
                            <div class="mb-6">
                                <h3 class="text-lg font-bold text-gray-700 mb-2">File Penugasan</h3>
                                <a href="/uploads/<%= assignment.path_file %>" download class="inline-flex items-center gap-3 bg-purple-100 border border-purple-300 rounded-lg px-4 py-3 hover:bg-purple-200 transition-colors">
                                    <svg class="h-6 w-6 text-purple-700" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path></svg>
                                    <span class="text-purple-800 font-medium"><%= assignment.nama_file %></span>
                                </a>
                            </div>
                        <% } %>

                        <div class="mb-8">
                            <h3 class="text-lg font-bold text-gray-700 mb-2">Tenggat Waktu</h3>
                            <div class="flex items-center gap-3 text-gray-600">
                                <svg class="w-6 h-6 text-red-500" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"></path></svg>
                                <span class="font-medium">
                                    <%= new Date(assignment.batas_waktu).toLocaleDateString('id-ID', {
                                        weekday: 'long', day: 'numeric', month: 'long', year: 'numeric',
                                        hour: '2-digit', minute: '2-digit'
                                    }) %>
                                </span>
                            </div>
                        </div>

                        <div class="border-t pt-6 flex justify-between items-center">
                            <a href="/assignments/<%= assignment.id %>/pengumpulan" class="inline-block bg-gray-600 hover:bg-gray-700 text-white font-bold py-3 px-6 rounded-lg transition-colors">
                                Lihat Pengumpulan
                            </a>
                            <a href="/assignments/<%= assignment.id %>/edit" class="inline-block bg-purple-600 hover:bg-purple-700 text-white font-bold py-3 px-8 rounded-lg transition-colors">
                                Edit Penugasan
                            </a>
                        </div>
                    </div>
                <% } else { %>
                    <p class="text-center text-white">Detail penugasan tidak ditemukan.</p>
                <% } %>
            </main>
        </div>
    </div>
</body>
</html>